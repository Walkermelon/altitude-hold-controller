#include "proportional.h"
float proportional(float currentAltitude , float setPoint) {
    // Placeholder for the actual proportional calculation
    float error = setPoint - currentAltitude;
    return error;  // Simple proportional gain (adjust as needed)
}